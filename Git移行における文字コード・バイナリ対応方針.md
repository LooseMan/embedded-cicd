# Git移行における文字コード・バイナリ対応方針

## 1. 目的

既存のソースコードをGitへ移行するにあたり、文字コードや改行コードが混在している既存資産を、移行時に一括修正することなく段階的に管理する。

特に、以下を実現することを目的とする。

* Merge Request（MR）上でソースコードをテキストとして差分比較できるようにする
* SJIS/CP932など、UTF-8以外の文字コードで記述されたソースもGit管理対象とする
* 初期段階では非ASCII文字の文字化けを許容する
* ASCII部分のソースコードや記号、改行についてはテキスト差分として比較可能にする
* バイナリファイルはテキストとして誤って比較しない
* 既存ソースの文字コードを一括変換しない
* 長期的には文字コードを検出し、`working-tree-encoding` を設定することでMR上の表示品質を改善する

---

## 2. 基本方針

移行を以下の段階に分けて実施する。

```text
Phase 1：MRで比較可能な状態を作る
    ↓
Phase 2：実際の文字コードを調査・記録する
    ↓
Phase 3：working-tree-encodingを設定する
    ↓
Phase 4：運用しながら対象を拡大する
```

重要なのは、最初からすべてのファイルの文字コードを正しく扱おうとしないことである。

まずは既存資産を低コストでGit管理へ移行し、MR上でレビュー可能な状態を作ることを優先する。

---

# 3. Phase 1：ソースコードを強制的にテキスト扱いする

## 3.1 方針

既存ソースにはSJIS/CP932など、GitLab等のMR画面で正常にUTF-8として表示できない文字コードが存在する。

この状態では、Gitの自動判定によってテキストではなくバイナリ扱いとなる可能性がある。

そのため、実際に使用されているソースコードの拡張子について、`.gitattributes`で明示的にテキスト扱いする。

例：

```gitattributes
*.c     text
*.h     text
*.cpp   text
*.hpp   text
*.s     text
*.asm   text
```

これにより、文字コードがUTF-8以外であっても、対象ファイルについてはテキストとして差分比較する。

---

## 3.2 初期段階で許容すること

Phase 1では、MR上で日本語コメント等が文字化けすることを許容する。

例えばSJISのソースがMR上で以下のように表示される場合がある。

```c
// �ʐM������

if (status == ERROR)
{
    retry();
}
```

この場合でも、以下のASCII部分をテキストとして比較できればよい。

* 英数字
* C/C++等のキーワード
* 変数名
* 関数名
* 演算子
* 記号
* インデント
* 改行

例えば、以下のような変更をMR上で確認できる状態を優先する。

```diff
- if (status == ERROR)
+ if (status != OK)
```

Phase 1の目的は、**文字コードを正しく表示することではなく、ソースコードをバイナリ扱いされずにテキスト差分として比較可能にすること**である。

---

# 4. 対象拡張子の決定方法

対象拡張子は、一般的な拡張子一覧を最初からすべて登録しない。

直近案件で実際に変更されたファイルをもとに決定する。

```text
直近案件の変更履歴
    ↓
変更されたファイル一覧を取得
    ↓
拡張子を抽出
    ↓
バイナリを除外
    ↓
実際に使用されている拡張子を特定
    ↓
.gitattributesへtext属性を追加
```

Gitの履歴から変更ファイルを取得する例：

```sh
git log --name-only --pretty=format:
```

重複を除外する場合：

```sh
git log --name-only --pretty=format: | sort -u
```

この結果から、実際に変更されているファイルの拡張子を集計する。

例えば、

```text
.c
.h
.cpp
.inc
.s
```

が確認された場合、必要な範囲のみ`.gitattributes`へ追加する。

```gitattributes
*.c     text
*.h     text
*.cpp   text
*.inc   text
*.s     text
```

これにより、移行初期から不要なファイルまで一律にテキスト扱いすることを避ける。

---

# 5. バイナリファイルの扱い

## 5.1 基本方針

バイナリファイルは原則としてGitのバージョン管理対象としない。

そのため、移行時にすべてのバイナリ形式を網羅して`.gitattributes`へ登録することは優先しない。

また、バイナリファイルの変更頻度も低いため、文字コード混在問題と同じレベルで複雑な仕組みを導入する必要はない。

---

## 5.2 判定方法

必要に応じて`file`コマンドを使用し、ファイル内容からテキストまたはバイナリを確認する。

例：

```sh
file target_file
```

MIMEタイプを確認する場合：

```sh
file -b --mime-type target_file
```

明らかなバイナリがGit管理対象に含まれている場合は、以下のいずれかを判断する。

1. Git管理対象から除外する
2. Git管理が必要な理由を確認する
3. 必要な場合のみ`.gitattributes`で`binary`を指定する

例：

```gitattributes
*.exe   binary
*.dll   binary
*.lib   binary
*.obj   binary
*.bin   binary
```

ただし、これは必要になった場合に追加する。

---

# 6. Phase 2：文字コードの実態を検出する

Phase 1でGitへの移行とMR上のテキスト比較を可能にした後、文字コードの実態を調査する。

目的は、既存ファイルを一括でUTF-8へ変換することではない。

各ファイルについて、実際にどの文字コードが使用されているかを把握する。

想定する分類例：

```text
UTF-8
CP932 / SJIS
EUC-JP
その他
判定不能
バイナリ
```

結果の例：

```text
src/foo.c       CP932
src/foo.h       CP932
src/bar.c       UTF-8
src/legacy.c    EUC-JP
src/data.bin    Binary
```

この情報をもとに、後続の`working-tree-encoding`設定対象を決定する。

---

# 7. Phase 3：working-tree-encodingを設定する

## 7.1 目的

長期的には、各ファイルをUTF-8へ一括変換するのではなく、Gitの`working-tree-encoding`を使用する。

これにより、

* 作業ツリー上の既存ファイルは従来の文字コードを維持する
* Gitリポジトリ内部ではUTF-8として管理する
* MRではUTF-8として正常に表示できる状態を目指す

---

## 7.2 CP932の例

例えば、`src/foo.c`がCP932であることが判明した場合、`.gitattributes`へ設定する。

```gitattributes
src/foo.c working-tree-encoding=CP932
```

`.h`ファイルも同様にCP932であれば設定する。

```gitattributes
src/foo.h working-tree-encoding=CP932
```

Gitの動作イメージは以下の通り。

```text
作業ツリー
CP932
    │
    │ git add
    ▼
Git内部
UTF-8
    │
    │ push
    ▼
MR
UTF-8として表示
    │
    │ checkout
    ▼
作業ツリー
CP932
```

既存の開発環境やツールがSJIS/CP932を前提としている場合でも、作業ツリー上の文字コードを変更せずにGit内部の表現を統一できる。

---

# 8. 文字コード検出結果のフィードバック

文字コード検出は、直ちにエラーとして移行を停止するためには使用しない。

まずは検出結果をフィードバックとして利用する。

例：

```text
WARNING: src/foo.c is CP932
WARNING: src/legacy.c is EUC-JP
WARNING: src/unknown.c encoding could not be determined
```

この結果をもとに、対象ファイルの文字コードを確認する。

確認できた場合は、`.gitattributes`へ`working-tree-encoding`を追加する。

```text
文字コード検出
    ↓
結果を通知
    ↓
文字コードを確認
    ↓
正しい文字コードを決定
    ↓
.gitattributesへ追加
    ↓
MR表示を改善
```

---

# 9. 最終的な運用方針

最終形では、以下のような段階的な運用を行う。

## Phase 1

```text
目的：すぐにMRでレビュー可能にする

実施内容：
・直近案件の変更実績から拡張子を抽出
・対象拡張子をtext扱い
・非ASCII文字の文字化けは許容
・ASCII部分をテキストdiffで確認
```

## Phase 2

```text
目的：既存資産の実態を把握する

実施内容：
・文字コードを検出
・UTF-8、CP932、EUC-JP等に分類
・バイナリを必要に応じてfileコマンドで確認
・結果をフィードバック
```

## Phase 3

```text
目的：MR上の文字コード表示を改善する

実施内容：
・文字コードが確認できたファイルへ
  working-tree-encodingを設定
・作業ツリーの既存文字コードは維持
・Git内部の表現をUTF-8へ統一
```

## Phase 4

```text
目的：運用しながら対象範囲を拡大する

実施内容：
・新しい拡張子がレビュー対象になった場合はtext属性を追加
・文字コードが判明したファイルは
  working-tree-encodingを追加
・必要な場合のみbinary属性を追加
```

---

# 10. 判断基準

本移行では、理想的な文字コード統一を最初から目指さない。

判断に迷った場合は、以下を優先する。

> **既存資産を壊さず、少ない移行コストでMR上のレビュー可能性を向上させる。**

したがって、

```text
最初から完全な文字コード管理
    ↓
移行コストが高い
    ↓
採用しない

まずtextとしてMR比較可能にする
    ↓
文字コードを検出する
    ↓
判明したものからworking-tree-encodingを設定
    ↓
段階的に表示品質を改善する
    ↓
採用する
```

という方針とする。
